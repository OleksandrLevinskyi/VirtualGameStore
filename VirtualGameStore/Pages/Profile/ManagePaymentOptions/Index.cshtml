@page
@model VirtualGameStore.Pages.Profile.ManagePaymentOptions.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PaymentOption[0].CardNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaymentOption[0].ExpiryDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaymentOption[0].HolderFirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaymentOption[0].HolderLastName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaymentOption[0].BillingAddress)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.PaymentOption) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CardNumber)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExpiryDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HolderFirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.HolderLastName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.BillingAddress)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
